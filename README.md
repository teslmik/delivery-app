# delivery-app
## Getting Started

First, run the development server:

for backend:
```bash
cd backend
npm install
npm run dev
# or
yarn dev
# or
pnpm dev
```

for frontend:
```bash
cd frontend
npm install
npm run dev
# or
yarn dev
# or
pnpm dev
```
